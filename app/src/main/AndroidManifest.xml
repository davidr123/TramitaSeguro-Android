<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tramitaseguro.sqlite.tramita_tramitador">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_iconotramita"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/react_native_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Notariales"></activity>
        <activity android:name=".PrincipalProvider" />
        <activity android:name=".Resultado" />
        <activity android:name=".Tramiteotros" />
        <activity android:name=".EstudioJuridicoActivity" />
        <activity android:name=".Transito" />
        <activity android:name=".TramitesTransito" />
        <activity android:name=".Bievenido" />
        <activity android:name=".Registro" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".UserProvider"
            android:authorities="tramitaseguro.sqlite.tramita_tramitador.UserProvider" />
    </application>

</manifest>